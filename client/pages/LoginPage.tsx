import { Text, View } from "react-native";

const LoginPage = () => {
  return (
    <View style={{ flex: 1, backgroundColor: "#212528" }}>
      <Text>로그인</Text>;
    </View>
  );
};

export default LoginPage;
